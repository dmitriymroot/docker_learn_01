version: "3.5"
services: 
         logcenter:
                build: logcenter/
                ports: 
                - 514:514
                volumes:
                - type: bind
                  source: /mnt/data
                  target: /mnt/data
         cron:          
                build: cron/
                volumes:
                - type: bind
                  source: /mnt/data        
                  target: /mnt/data

